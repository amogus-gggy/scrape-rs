[package]
name = "test"
version = "0.1.0"
edition = "2024"
publish = false

[dependencies]
ureq = { version = "3.4.0", features = ["json"] }
serde = { version = "1.0", features = ["derive"] }
scrape-rs = { path = "../scrape-rs" }
scraper = "0.20"
dhat = "0.3.3"
mimalloc = "0.1"

[features]
dhat-heap = []

